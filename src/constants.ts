export enum ChartType {
    overtime = 'overtime',
    snapshot = 'snapshot'
}

export const chartViewOptions = [
    { value: 'overtime', label: 'Allocation over time' },
    { value: 'snapshot', label: 'Allocation snapshot' }
];
